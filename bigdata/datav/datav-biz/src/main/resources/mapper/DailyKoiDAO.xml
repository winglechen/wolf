<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.daydayup.wolf.bigdata.datav.biz.dal.dao.DailyKoiDAO">


<!--auto generated by MybatisCodeHelper on 2020-03-26-->
    <select id="selectByDate" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from daily_koi
        where orgId = #{request.orgId, jdbcType=BIGINT}
        <if test="request.dateAfter != null">
            and `date` >= #{request.dateAfter,jdbcType=DATE}
        </if>
        <if test="request.dateBefore != null">
            and `date` <= #{request.dateBefore,jdbcType=DATE}
        </if>
        order by id desc
    </select>
</mapper>