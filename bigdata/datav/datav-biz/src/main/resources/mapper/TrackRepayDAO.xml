<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.daydayup.wolf.bigdata.datav.biz.dal.dao.TrackRepayDAO">


<!--auto generated by MybatisCodeHelper on 2020-03-26-->
    <select id="selectByTrack" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from track_repay
        where org_id=#{request.orgId,jdbcType=BIGINT}
        <if test="request.goodsId != null">
            and goods_id=#{request.goodsId,jdbcType=BIGINT}
        </if>
        <if test="request.installmentNo != null">
            and installment_no=#{request.installmentNo,jdbcType=INTEGER}
        </if>
        <if test="request.startDate != null">
            <![CDATA[ and `due_date` >= #{request.startDate,jdbcType=DATE} ]]>
        </if>
        <if test="request.endDate != null">
            <![CDATA[ and `due_date` <= #{request.endDate,jdbcType=DATE} ]]>
        </if>
        order by id desc
    </select>
</mapper>